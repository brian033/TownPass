# 列車擁擠度判斷與運動篩選功能說明

## 功能概述

此功能會根據捷運列車的即時擁擠度資訊，自動篩選適合在車廂內進行的運動。當車廂擁擠時（平均擁擠度 > 2），只會推薦 `doInCrowded: true` 的運動；當車廂不擁擠時，所有運動都可以推薦。

## 實作邏輯

### 1. 擁擠度判斷流程

1. **取得路徑資訊**：從起始站到第一個中繼站的路徑（例如：江子翠 → 龍山寺）

2. **查詢站名編號**：
   - 從 `mrt_station_inshort.json` 取得兩站的編號
   - 例如：江子翠 = `["BL09"]`，龍山寺 = `["BL10"]`

3. **判斷行車方向**：
   - 比較站名編號的數字部分
   - BL09 → BL10：編號增加 = **上行**
   - BL10 → BL09：編號減少 = **下行**

4. **尋找最近列車**：
   - 優先：檢查起始站是否有列車（匹配方向 + 下一站）
   - 次選：往前找 1-5 站，直到找到符合方向的列車
   - 上行時往前找：BL09 → BL08 → BL07 → ...
   - 下行時往後找：BL09 → BL10 → BL11 → ...

5. **計算擁擠度**：
   - 從 `train_crowding.json` 取得該列車資訊
   - 計算所有車廂擁擠度的平均值
   - 判斷：平均值 > 2 = 擁擠

6. **篩選運動**：
   - 擁擠時：只保留 `doInCrowded: true` 的運動
   - 不擁擠時：所有運動都可用

## 檔案結構

### 新增檔案

#### `lib/service/train_crowding_service.dart`
列車擁擠度服務，包含：
- `TrainInfo`：列車資訊（含車廂擁擠度）
- `CarriageInfo`：車廂資訊
- `TrainCrowdingService`：擁擠度檢查服務
  - `initialize()`：載入資料
  - `checkIfCrowded(fromStation, toStation)`：檢查路徑是否擁擠
  - `filterExercisesByCrowding(exercises, isCrowded)`：篩選運動

### 修改檔案

#### `lib/page/new_component/new_component_view_controller.dart`
- 新增：`TrainCrowdingService` 實例
- 修改：`loadData()` 加入服務初始化
- 修改：`_computeRoute()` 加入擁擠度檢查和運動篩選

## 資料檔案

### `assets/mock_data/train_crowding.json`
列車即時擁擠度資料：
```json
{
  "trains": [
    {
      "trainNumber": "B201",
      "direction": "上行",
      "currentStation": "江子翠",
      "currentStationCode": "BL09",
      "carriages": [
        {
          "carriageNumber": 1,
          "crowdingLevel": 4,
          "crowdingLevelText": "非常擁擠"
        }
      ],
      "nextStation": "龍山寺"
    }
  ]
}
```

### `assets/mock_data/mrt_station_inshort.json`
站名編號對照表：
```json
{
  "江子翠": ["BL09"],
  "龍山寺": ["BL10"],
  "台北車站": ["R10", "BL12"]
}
```

### `assets/mock_data/exercise.json`
運動資料（含擁擠度適用性）：
```json
[
  {
    "name": "Standing Lateral Stretch",
    "doInCrowded": false,
    "parts": ["upper_body", "core"]
  },
  {
    "name": "Standing Pelvic Tilt",
    "doInCrowded": true,
    "parts": ["core", "lower_body"]
  }
]
```

## 擁擠度等級

| 等級 | 名稱 | 載客率 | 顏色 |
|------|------|--------|------|
| 1 | 寬鬆 | 0-40% | 綠色 |
| 2 | 舒適 | 41-70% | 淺綠 |
| 3 | 擁擠 | 71-90% | 橘色 |
| 4 | 非常擁擠 | 91-120% | 紅色 |

**判斷標準**：平均擁擠度 > 2 時視為擁擠

## 使用範例

```dart
// 在 NewComponentViewController 中
final route = _computeRoute(startId, endId);
// route.exercises 已根據擁擠度自動篩選

// 直接使用服務
final crowdingService = TrainCrowdingService();
await crowdingService.initialize();

final isCrowded = crowdingService.checkIfCrowded('江子翠', '龍山寺');
// isCrowded = true (因為 B201 列車平均擁擠度 = 3.83 > 2)

final exercises = [...]; // 所有運動
final filtered = crowdingService.filterExercisesByCrowding(exercises, isCrowded);
// filtered 只包含 doInCrowded: true 的運動
```

## 測試情境

### 情境 1：江子翠 → 龍山寺（上行）
- 找到列車：B201（在江子翠，往龍山寺）
- 平均擁擠度：3.83
- 判斷：擁擠
- 結果：只推薦 `doInCrowded: true` 的運動

### 情境 2：龍山寺 → 江子翠（下行）
- 找到列車：B203（在龍山寺，往江子翠）
- 平均擁擠度：2.17
- 判斷：稍微擁擠
- 結果：只推薦 `doInCrowded: true` 的運動

### 情境 3：找不到列車
- 預設：不擁擠
- 結果：推薦所有運動

## Debug 訊息

執行時會輸出以下 debug 訊息：
```
Station codes not found for XXX or YYY
No common line found between XXX and YYY
Could not determine direction
No train found for route XXX -> YYY
Train B201 from 江子翠 to 龍山寺: avg crowding = 3.83, isCrowded = true
Route from 江子翠 to 龍山寺: isCrowded = true, available exercises = 5
```

## 注意事項

1. **資料即時性**：目前使用 mock data，實際應用需要接入即時 API
2. **快取考量**：擁擠度會隨時間變化，不應長時間快取結果
3. **轉乘站處理**：轉乘站會有多個編號，程式會自動找出共同路線
4. **列車搜尋範圍**：最多往前/後找 5 站，可依需求調整
5. **預設行為**：找不到資料時預設為「不擁擠」，確保使用者體驗

## 未來擴充

1. 整合即時 API 取代 mock data
2. 支援多段路徑的擁擠度檢查
3. 顯示擁擠度資訊在 UI 上
4. 提供「等下一班」的建議
5. 記錄歷史擁擠度模式
