# 新增服務卡片元件文檔

## 📋 概述

在「服務」頁面的官方服務卡片區塊中新增了第五張橫向卡片元件，可點擊跳轉到新頁面。

---

## 📁 相關檔案清單

### 新建檔案

1. **`lib/page/city_service/widget/official_service_card/official_service_card_new.dart`**
   - 第五張卡片的元件實作

2. **`lib/page/new_component/new_component_view.dart`**
   - 點擊卡片後跳轉的目標頁面

### 修改檔案

3. **`lib/page/city_service/widget/official_service_card/official_service_card_widget.dart`**
   - 官方服務卡片的容器元件（包含所有五張卡片）

4. **`lib/util/tp_route.dart`**
   - 路由配置檔案

---

## 🎨 UI 風格規範

### 卡片樣式

新卡片使用**白色背景風格**（與「有話要說」、「警政報案」、「防疫醫療」、「市政APP」相同）：

- **背景色**：`TPColors.white`
- **邊框**：`TPColors.grayscale100`（淡灰色，1px）
- **圓角**：`6.0` 圓角半徑
- **內邊距**：`horizontal: 12, vertical: 16`

### 文字樣式

遵循既有白色卡片的文字風格：

- **主標題（中文）**
  - 樣式：`TPTextStyles.h3SemiBold`
  - 顏色：`TPColors.primary500`（品牌藍色）
  - 溢出處理：`TextOverflow.ellipsis`

- **副標題（英文）**
  - 樣式：`TPTextStyles.caption`
  - 顏色：`TPColors.grayscale700`（深灰色）
  - 溢出處理：`TextOverflow.ellipsis`

### Icon 規範

- **尺寸**：`Size.square(48)` (48x48)
- **來源**：`Assets.svg.iconXxx.svg()`
- **位置**：文字右側

### 佈局結構

```
Row
├─ Expanded (文字區域)
│  └─ Column
│     ├─ 主標題（中文）
│     └─ 副標題（英文）
└─ SizedBox.fromSize (Icon 區域)
```

---

## ⚙️ 元件架構

### 繼承關係

```
OfficialServiceCard (抽象基類)
├─ OfficialServiceCardTopLeft (左上：市政服務)
├─ OfficialServiceCardTopRight (右上：有話要說、警政報案)
├─ OfficialServiceCardBottomLeft (左下：防疫醫療、市政APP)
├─ OfficialServiceCardBottomRight (右下：城市生活)
└─ OfficialServiceCardNew (新增：新元件) ← 新增的
```

### 基類說明

所有卡片都繼承自 `OfficialServiceCard`，位於：
- **檔案**：`lib/page/city_service/widget/official_service_card/official_service_card.dart`
- **必須實作**：`Widget layoutBuild(BuildContext context, BoxConstraints constraint)`
- **可覆寫**：`Color get borderColor`（預設：`Colors.transparent`）

---

## 🔧 常見修改指南

### 1. 修改卡片文字

**檔案**：`lib/page/city_service/widget/official_service_card/official_service_card_new.dart`

```dart
// 找到這段程式碼（約第 34-43 行）
const TPText(
  '新元件',  // ← 修改主標題
  style: TPTextStyles.h3SemiBold,
  color: TPColors.primary500,
  overflow: TextOverflow.ellipsis,
),
const TPText(
  'New Component',  // ← 修改副標題
  style: TPTextStyles.caption,
  color: TPColors.grayscale700,
  overflow: TextOverflow.ellipsis,
),
```

### 2. 更換 Icon

**檔案**：`lib/page/city_service/widget/official_service_card/official_service_card_new.dart`

**可用的 Icon 清單**：查看 `assets/svg/` 資料夾，常用的有：
- `Assets.svg.iconMore.svg()` - 更多（目前使用）
- `Assets.svg.iconInfo.svg()` - 資訊
- `Assets.svg.iconSettings.svg()` - 設定
- `Assets.svg.iconStore.svg()` - 商店
- `Assets.svg.iconExploreTaipei.svg()` - 探索
- 等等...

```dart
// 找到這段程式碼（約第 53-56 行）
SizedBox.fromSize(
  size: const Size.square(48),
  child: Assets.svg.iconMore.svg(),  // ← 修改這裡
),
```

### 3. 修改跳轉目標

**檔案**：`lib/page/city_service/widget/official_service_card/official_service_card_new.dart`

```dart
// 找到這段程式碼（約第 19 行）
onTap: () async {
  await TPRoute.openUri(uri: 'local://new_component');  // ← 修改路由
},
```

**路由格式**：
- **本地頁面**：`local://路由名稱`（例如：`local://new_component`）
- **外部網頁**：`https://網址`（例如：`https://taipei-pass-service.vercel.app/...`）

### 4. 在新頁面增加內容

**檔案**：`lib/page/new_component/new_component_view.dart`

**基本結構**：
```dart
Scaffold(
  backgroundColor: TPColors.white,  // 背景色
  appBar: const TPAppBar(
    title: '新元件',  // ← App Bar 標題
  ),
  body: // ← 在這裡增加內容
)
```

**常用元件參考**：

```dart
// 範例 1：簡單文字內容
body: const Padding(
  padding: EdgeInsets.all(24.0),
  child: Column(
    children: [
      TPText(
        '標題文字',
        style: TPTextStyles.h2SemiBold,
        color: TPColors.grayscale900,
      ),
      SizedBox(height: 16),
      TPText(
        '內文',
        style: TPTextStyles.bodyRegular,
        color: TPColors.grayscale700,
      ),
    ],
  ),
),

// 範例 2：可滾動列表
body: ListView(
  padding: const EdgeInsets.all(16),
  children: [
    TPCard(...),  // 卡片元件
    const SizedBox(height: 8),
    TPCard(...),
  ],
),

// 範例 3：使用 WebView
body: InAppWebView(
  initialUrlRequest: URLRequest(
    url: WebUri('https://your-url.com'),
  ),
),
```

### 5. 修改頁面標題

**檔案**：`lib/page/new_component/new_component_view.dart`

```dart
// 找到這段程式碼（約第 13-15 行）
appBar: const TPAppBar(
  title: '新元件',  // ← 修改這裡
),
```

### 6. 新增頁面需要狀態管理（Controller）

如果頁面需要複雜的狀態管理，參考其他頁面的做法：

**步驟 1**：創建 Controller
- **檔案**：`lib/page/new_component/new_component_view_controller.dart`

```dart
import 'package:get/get.dart';

class NewComponentViewController extends GetxController {
  // 在這裡定義狀態和方法
  final RxString exampleState = ''.obs;

  @override
  void onInit() {
    super.onInit();
    // 初始化邏輯
  }
}
```

**步驟 2**：修改 View 使用 Controller
- **檔案**：`lib/page/new_component/new_component_view.dart`

```dart
import 'package:get/get.dart';
import 'package:town_pass/page/new_component/new_component_view_controller.dart';

class NewComponentView extends GetView<NewComponentViewController> {
  const NewComponentView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // ... 使用 controller.xxx 存取狀態
    );
  }
}
```

**步驟 3**：在路由中註冊 Controller
- **檔案**：`lib/util/tp_route.dart`

```dart
// 1. 添加 import
import 'package:town_pass/page/new_component/new_component_view_controller.dart';

// 2. 修改 GetPage（約第 124-127 行）
GetPage(
  name: newComponent,
  page: () => const NewComponentView(),
  binding: BindingsBuilder(() {
    Get.put<NewComponentViewController>(NewComponentViewController());
  }),
),
```

---

## 🎯 設計原則

### 顏色系統

主要使用 `TPColors` 類別：

- **品牌色（Primary）**：`primary500` - 主要互動元素
- **灰階（Grayscale）**：`grayscale50` ~ `grayscale950` - 背景、邊框、文字
- **白色**：`white` - 卡片背景
- **其他**：`red500`、`orange500`、`secondary500` 等

### 文字樣式

主要使用 `TPTextStyles` 類別：

- **標題**：`h1SemiBold`, `h2SemiBold`, `h3SemiBold`
- **內文**：`bodyRegular`, `bodyMedium`, `bodySemiBold`
- **小字**：`caption`, `captionMedium`

### 間距規範

- **小間距**：`4.0`, `8.0`
- **標準間距**：`12.0`, `16.0`
- **大間距**：`20.0`, `24.0`
- **區塊間距**：`50.0`

---

## 📍 頁面在 App 中的位置

```
MainView (底部導航)
└─ CityServiceView (服務 Tab)
   └─ body: ListView
      ├─ PinnedServiceWidget (我的服務)
      ├─ OfficialServiceCardWidget ← 包含新卡片
      │  ├─ Table (4x4 卡片)
      │  └─ OfficialServiceCardNew ← 新卡片
      └─ TrendingServiceWidget (熱門服務)

點擊新卡片 → NewComponentView (新元件頁面)
```

---

## 🔗 重要參考檔案

### UI 工具類
- **`lib/util/tp_colors.dart`** - 顏色定義
- **`lib/util/tp_text.dart`** - 文字元件
- **`lib/util/tp_app_bar.dart`** - App Bar 元件
- **`lib/util/tp_card.dart`** - 卡片元件
- **`lib/util/tp_constant.dart`** - 常數定義（如 `goldenRatio`）

### 類似頁面範例
- **`lib/page/online_police/online_police_view.dart`** - 包含卡片列表的頁面
- **`lib/page/message/message_view.dart`** - 使用 Tab 的複雜頁面
- **`lib/page/feedback/feedback_view.dart`** - 簡單表單頁面

### 其他卡片參考
- **`lib/page/city_service/widget/official_service_card/official_service_card_top_right.dart`** - 包含兩個子項目的白色卡片
- **`lib/page/city_service/widget/official_service_card/official_service_card_bottom_left.dart`** - 相同風格的白色卡片

---

## ✅ 快速修改檢查表

修改卡片文字時：
- [ ] 修改 `official_service_card_new.dart` 中的 TPText 內容
- [ ] 確認文字長度不會導致溢出

更換 Icon 時：
- [ ] 確認 Icon 檔案存在於 `assets/svg/` 中
- [ ] 使用 `Assets.svg.iconXxx.svg()` 格式
- [ ] Icon 尺寸建議保持 48x48

修改跳轉目標時：
- [ ] 如果是新頁面，需在 `tp_route.dart` 中添加路由
- [ ] 使用 `local://路由名稱` 格式

新增頁面內容時：
- [ ] 保持 `TPColors.white` 背景
- [ ] 使用 `TPText` 而非原生 `Text`
- [ ] 遵循既有的 padding 和間距規範（16, 24）
- [ ] 需要互動時考慮使用 GetX Controller

---

## 📝 版本記錄

### v1.0.0 - 2025-11-07
- ✅ 創建第五張服務卡片元件
- ✅ 新增新元件目標頁面
- ✅ 整合至服務頁面佈局
- ✅ 配置路由導航

