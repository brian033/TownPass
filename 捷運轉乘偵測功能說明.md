# 捷運轉乘偵測功能說明

## 📋 功能概述

當使用者在運動推薦頁面進行跨線旅程時，系統會自動偵測是否需要轉乘，並提供轉乘暫停功能，讓使用者可以在轉乘時暫停計時。

## 🎯 功能特點

### 1. 自動偵測轉乘站
- 系統會自動識別有兩個或以上路線代碼的車站（轉乘站）
- 共有 **轉乘站數量** 個轉乘站被系統識別

### 2. 智能判斷是否需要轉乘
系統會檢查以下條件來判斷是否需要轉乘：
- ✅ 當前站是否為轉乘站
- ✅ 前一站的路線代碼
- ✅ 下一站的路線代碼
- ✅ 比對路線代碼的英文字母部分

**範例：西門 → 台北車站 → 台大醫院**
```
西門站代碼：    ["G12", "BL11"]  → 路線：G, BL
台北車站代碼：  ["R10", "BL12"]  → 轉乘站
台大醫院代碼：  ["R09"]          → 路線：R

判斷：
- 台北車站是轉乘站 ✓
- 西門的路線：G, BL
- 台大醫院的路線：R
- 沒有共同路線 → 需要轉乘！
```

### 3. 轉乘暫停功能
當偵測到需要轉乘時：
1. 🔶 顯示橘色轉乘提示框
2. ⏸️ 自動暫停計時
3. 🔘 顯示「繼續」按鈕（橘色）
4. ℹ️ 顯示轉乘資訊（從哪條線轉到哪條線）

使用者操作：
- 按下「繼續」按鈕 → 恢復計時，按鈕變成綠色「暫停」
- 再次按下「暫停」→ 再次暫停計時，按鈕變回橘色「繼續」

## 🔧 技術架構

### 核心服務：`MrtTransferService`

#### 單例模式
```dart
final transferService = MrtTransferService();
```

#### 主要方法

##### 1. `initialize()`
初始化轉乘站資料，從 `mrt_station_inshort.json` 讀取並篩選轉乘站。

##### 2. `isTransferStation(String stationName)`
檢查某個站點是否為轉乘站。

##### 3. `needsTransfer({currentStation, previousStation, nextStation})`
判斷是否需要轉乘（返回 true/false）。

##### 4. `getTransferInfo({currentStation, previousStation, nextStation})`
獲取詳細的轉乘資訊（返回 `TransferInfo?`）。

### 整合位置：`JourneyTrackerWidget`

#### 新增狀態變數
```dart
final MrtTransferService _transferService = MrtTransferService();
bool _isTransferPaused = false;              // 是否處於轉乘暫停狀態
bool _needsTransferAtCurrentStation = false; // 當前站是否需要轉乘
TransferInfo? _currentTransferInfo;          // 當前轉乘資訊
```

#### 核心方法

##### `_checkTransferAtCurrentStation()`
每次到達新站時調用，檢查是否需要轉乘：
- 獲取當前站、前一站、下一站
- 調用 `MrtTransferService.getTransferInfo()`
- 如果需要轉乘，自動暫停計時並更新 UI

##### `_handleTransferPause()`
處理轉乘暫停/繼續按鈕的點擊：
- 切換 `_isTransferPaused` 狀態
- 如果繼續：啟動計時器
- 如果暫停：停止計時器

## 📊 轉乘站列表

系統自動識別的轉乘站包括（範例）：
- 台北車站：R10, BL12（紅線 ↔ 藍線）
- 中山：R11, G14（紅線 ↔ 綠線）
- 大安：BR09, R05（文湖線 ↔ 紅線）
- 忠孝復興：BR10, BL15（文湖線 ↔ 藍線）
- 南京復興：BR11, G16（文湖線 ↔ 綠線）
- 古亭：G09, O05（綠線 ↔ 橘線）
- 東門：R07, O06（紅線 ↔ 橘線）
- 西門：G12, BL11（綠線 ↔ 藍線）
- ... 等等

（完整列表由系統從 `mrt_station_inshort.json` 自動生成）

## 🎨 UI 設計

### 轉乘提示框
- 背景色：橘色 50（淡橘色）
- 邊框：橘色 300
- 圖示：轉乘圖示（transfer_within_a_station）
- 內容：
  - 轉乘站名稱
  - 路線資訊（例如：BL/G 線 → R 線）

### 按鈕佈局
```
[上一站]  [轉乘暫停/繼續]  [下一站]
  灰色       橘色/綠色         藍色
```

### 按鈕狀態
- **暫停狀態**（橘色）：顯示「繼續」+ 播放圖示 ▶
- **運行狀態**（綠色）：顯示「暫停」+ 暫停圖示 ⏸

## 💡 使用範例

### 範例 1：西門 → 台北車站 → 台大醫院
1. 使用者在西門站開始旅程
2. 點擊「下一站」到達台北車站
3. **系統自動偵測**：
   - 台北車站是轉乘站 ✓
   - 前一站（西門）：綠線/藍線
   - 下一站（台大醫院）：紅線
   - 需要轉乘！
4. **UI 變化**：
   - 顯示橘色轉乘提示框
   - 計時器自動暫停
   - 顯示「繼續」按鈕（橘色）
5. 使用者完成轉乘後，點擊「繼續」
6. 計時器恢復，繼續運動

### 範例 2：中山 → 雙連 → 劍潭（同線旅程）
1. 使用者從中山站開始
2. 雖然中山是轉乘站，但前一站和下一站都在紅線上
3. **系統判斷**：不需要轉乘
4. **UI 保持**：正常的「上一站」「下一站」按鈕
5. 計時器持續運行

## 🔍 偵測邏輯詳解

### 路線代碼提取
```dart
// 從 "R10" 提取 "R"
// 從 "BL12" 提取 "BL"
String extractLineId(String code) {
  return code.replaceAll(RegExp(r'[0-9]'), '');
}
```

### 共同路線檢查
```dart
bool hasCommonLine(List<String> codes1, List<String> codes2) {
  final lines1 = codes1.map(extractLineId).toSet(); // {G, BL}
  final lines2 = codes2.map(extractLineId).toSet(); // {R}
  return lines1.intersection(lines2).isNotEmpty;    // false → 需要轉乘
}
```

## 📝 注意事項

1. **終點站不檢查轉乘**：到達終點站時不會觸發轉乘檢查
2. **起點站不檢查轉乘**：在起點站時不會觸發轉乘檢查
3. **自動暫停**：偵測到轉乘時會自動暫停，無需手動操作
4. **計時累計**：暫停期間不計時，只計算實際運動時間

## 🚀 未來擴展

可能的改進方向：
- [ ] 顯示轉乘路線圖
- [ ] 預估轉乘所需時間
- [ ] 轉乘站樓層資訊
- [ ] 語音提示轉乘資訊
- [ ] 轉乘歷史記錄

## 📚 相關檔案

- **服務類別**：`lib/service/mrt_transfer_service.dart`
- **UI 整合**：`lib/page/exercise_recommendation/widget/journey_tracker_widget.dart`
- **資料來源**：`assets/mock_data/mrt_station_inshort.json`
- **功能說明**：本檔案
